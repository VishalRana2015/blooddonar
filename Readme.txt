The project has been downloaded from Spring Initializr
The following dependencies have been specified in pom.xml file.
spring-boot-starter-data-jpa : Used to write JPA queries.
spring-boot-starter-web : Used as a basic project which have all the necessary dependency this is required to create a spring boot project.
 So that you don't have to declare them everytime when you need to create a new spring project. So you just specifiy this artifact in your pom.xml file.
 spring-boot-devtools : Development tools.
 postgresql : postgresql jdbc API
 lombok : Used to give definations to default constructor getter and setter with just annotations. Save a lot of time.
 spring-boot-starter-test

 -------------------------------------------------------------------------------